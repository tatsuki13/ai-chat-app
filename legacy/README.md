# Legacy files

このフォルダは、現在のNext.jsルーティングから外して保管した旧実装の置き場です。

- `app-page.legacy.js`: 旧 `app/page.js` の退避コピー

`app/page.js` と `app/page.tsx` を同じ階層に置くと、どちらも `/` のページ候補になり、更新やビルド時の衝突原因になります。必要になった場合だけ中身を参照し、現在の画面へ移植してください。
