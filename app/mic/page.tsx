import RemoteMicClientShell from "./remote-mic-client-shell";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function RemoteMicPage() {
  return <RemoteMicClientShell />;
}
